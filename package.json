{
  "name": "coffee-run",
  "version": "1.0.0",
  "description": "Calculate who should pay for coffee next",
  "main": "dist/src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf ./dist",
    "dev": "npm run clean && tsc --sourceMap && node dist/src/index.js",
    "build": "npm run clean && tsc"
  },
  "author": "Mark Brooks",
  "license": "ISC",
  "keywords": [
    "cli"
  ],
  "dependencies": {
    "chalk": "^4.1.2",
    "fs": "^0.0.1-security",
    "readline-sync": "^1.4.10"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/readline-sync": "^1.4.8",
    "typescript": "^5.4.3"
  }
}
